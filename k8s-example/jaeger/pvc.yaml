apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jaeger-elasticsearch
  namespace: observability
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: gp2
